function processParameters(param1: string, param2: number, param3: boolean): void {
  // Process the parameters here
  console.log(`String parameter: ${param1}`);
  console.log(`Number parameter: ${param2}`);
  console.log(`Boolean parameter: ${param3}`);
}

// Example usage:
processParameters('example', 42, true);
